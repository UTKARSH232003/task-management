{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "hlo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && npm install --prefix frontend && cd frontend && npx ng build",
    "start": "node backend/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
